extends layout

block body
  div#content
    div#splash
      img#homeImage1(src="/images/background.png")
      p #{__('home.splash')}

    script.
      $(document).ready(function()
      {
        $('#splash').hide().fadeIn(1500);
      });